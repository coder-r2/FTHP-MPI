#!/bin/bash

export PAREP_MPI_WORKDIR=/scratch/risheg/Fault-Tolerant-MPI
export FC=gfortran

###echo $CC -I$PAREP_MPI_WORKDIR/src -fno-stack-protector $@ -L$PAREP_MPI_WORKDIR/lib -lmpi -ldl
$FC -I$PAREP_MPI_WORKDIR/src -fno-stack-protector $@ -L$PAREP_MPI_WORKDIR/lib -lmpi -ldl -cpp
